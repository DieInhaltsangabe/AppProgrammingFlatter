import 'package:flutter/material.dart';

// localization class, which will connect everything to the three languages we translated here.
// The language strings are in the .arb files
class L10n {
  static final all = [
    const Locale('en'),
    const Locale('de'),
    const Locale('tr')
  ];
}
